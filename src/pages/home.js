import React from 'react';
import StudentsList from '../components/StudentsList';

const Home = () => {
    return (
        <div>
            <StudentsList/>
        </div>
    );
};

export default Home;